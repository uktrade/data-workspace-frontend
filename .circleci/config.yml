version: 2
jobs:
  build:
    docker:
      # Test and code run in _other_ docker containers, so mostly doesn't matter
      - image: circleci/python:3.7
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run tests
          command: |
            touch .env
            make docker-test
